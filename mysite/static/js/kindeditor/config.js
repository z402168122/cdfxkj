KindEditor.ready(function(K) {
    window.editor = K.create('#id_detail1',{

        // 指定大小
        width:'800px',
        height:'200px',
    });
});


KindEditor.ready(function(K) {
    window.editor = K.create('#id_detail2',{

        // 指定大小
        width:'800px',
        height:'200px',
    });
});


KindEditor.ready(function(K) {
    window.editor = K.create('#id_detail3',{

        // 指定大小
        width:'800px',
        height:'200px',
    });
});


KindEditor.ready(function(K) {
    window.editor = K.create('#id_detail4',{

        // 指定大小
        width:'800px',
        height:'200px',
    });
});